import { ArrowUpRight, Database, FileText, FlaskConical } from "lucide-react";
import { Link } from "react-router-dom";
import { useAuth } from "../contexts/AuthContext";
export function Dashboard() { const { session } = useAuth(); return <div className="page"><div className="page-heading"><div><p className="eyebrow">Overview</p><h1>Good to see you, {session?.email.split("@")[0]}.</h1><p className="muted">Your data workspace, ready when you are.</p></div><Link className="button primary" to="/datasets">Open datasets <ArrowUpRight size={16} /></Link></div><div className="metric-grid"><div className="metric-card accent"><span><Database size={18} />Datasets</span><strong>—</strong><small>Upload your first source</small></div><div className="metric-card"><span><FlaskConical size={18} />Experiments</span><strong>—</strong><small>Runs will appear here</small></div><div className="metric-card"><span><FileText size={18} />Reports</span><strong>—</strong><small>Generated insights</small></div></div><section className="welcome-panel"><div><p className="eyebrow">Start here</p><h2>Bring a dataset into focus.</h2><p>Upload a CSV or spreadsheet to begin profiling, quality checks, and preparation.</p></div><Link className="button secondary" to="/datasets">View datasets <ArrowUpRight size={16} /></Link></section></div>; }
